{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter './artifacts/**' --filter './scripts' run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
