{
  "name": "workspace",
  "private": true,
  "scripts": {
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter './artifacts/**' run typecheck --if-present && pnpm -r --filter './scripts/**' run typecheck --if-present"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
